#!/bin/bash

# This mainly exists for documentation purposes...
# You can start and stop the ES container as you wish

docker-compose up -d elasticsearch-test
clojure -A:dev:test
docker-compose stop elasticsearch-test

